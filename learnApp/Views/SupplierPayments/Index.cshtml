@model IEnumerable<learnApp.Models.SupplierPayment>
@using learnApp.Helper
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaymentDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Supplier.SupplierName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @CurrencyHelper.ToVnd(item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaymentDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supplier.SupplierName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SupplierPaymentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SupplierPaymentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SupplierPaymentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
